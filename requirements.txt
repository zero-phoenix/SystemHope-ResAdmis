# SystemHope ResAdmis - dependencias REALES del utillaje de scripts/ (23/09/2026).
# Versiones probadas con Python 3.10 a 3.12. Sin OCR: ninguna libreria de OCR
# (pytesseract, easyocr, paddleocr, ocrmypdf, rapidocr) entra jamas aqui (R-137).
#
#   pip install -r requirements.txt

PyMuPDF==1.28.0       # paginas reales, captura completa de cada pagina y contraste (extraer_expediente, admisorio)
pypdf==6.19.0         # respaldo de lectura de PDF (extraer_expediente)
python-docx==1.2.0    # inspeccion y pruebas de .docx
lxml==6.1.1           # dependencia de python-docx
Pillow==12.3.0        # sello de procedencia de imagenes (guardia_admisorio)
openpyxl==3.1.5       # padron de casillas electronicas (filtrar_casillas)
